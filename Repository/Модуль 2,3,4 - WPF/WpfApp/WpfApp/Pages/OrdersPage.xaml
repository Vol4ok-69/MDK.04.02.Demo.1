<Page x:Class="WpfApp.Pages.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Заказы"
      FontFamily="Calibri"
      Background="#FFFFFF">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#DAA520"
                Padding="15">

            <Grid>

                <TextBlock Text="Список заказов"
                           FontSize="26"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">

                    <StackPanel Margin="0,0,20,0">

                        <TextBlock Text="{Binding FullName, Mode=OneWay}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Right"/>

                        <TextBlock Text="{Binding RoleName, Mode=OneWay}"
                                   FontSize="14"
                                   HorizontalAlignment="Right"/>

                    </StackPanel>

                    <Button Content="Добавить заказ"
                            Width="140"
                            Height="35"
                            Margin="0,0,10,0"
                            Background="#B8860B"
                            Foreground="White"
                            Visibility="{Binding IsAdmin,
                                                 Converter={StaticResource BooleanToVisibilityConverter}}"
                            Command="{Binding AddOrderCommand}"/>

                    <Button Content="Назад"
                            Width="100"
                            Height="35"
                            Background="#B8860B"
                            Foreground="White"
                            Command="{Binding BackCommand}"/>

                </StackPanel>

            </Grid>

        </Border>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Orders, Mode=OneWay}"
                 Margin="20"
                 BorderThickness="0"
                 Background="White"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">

                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch"/>

                    <Setter Property="Padding"
                            Value="0"/>

                    <Setter Property="Margin"
                            Value="0,0,0,10"/>

                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>

                    <Border BorderBrush="LightGray"
                            BorderThickness="1"
                            Padding="12"
                            Background="White">

                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="190"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">

                                <TextBlock FontSize="18"
                                           FontWeight="Bold"
                                           Margin="0,0,0,5">

                                    <Run Text="Номер заказа: "/>
                                    <Run Text="{Binding Code, Mode=OneWay}"/>

                                </TextBlock>

                                <TextBlock FontSize="15"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,5">

                                    <Run Text="Товары: "/>
                                    <Run Text="{Binding ProductsSummary, Mode=OneWay}"/>

                                </TextBlock>

                                <TextBlock FontSize="15"
                                           Margin="0,0,0,5">

                                    <Run Text="Клиент: "/>
                                    <Run Text="{Binding CustomerName, Mode=OneWay}"/>

                                </TextBlock>

                                <TextBlock FontSize="15"
                                           Margin="0,0,0,5">

                                    <Run Text="Статус: "/>
                                    <Run Text="{Binding StatusName, Mode=OneWay}"/>

                                </TextBlock>

                                <TextBlock FontSize="15"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,5">

                                    <Run Text="Пункт выдачи: "/>
                                    <Run Text="{Binding PickupPointAddress, Mode=OneWay}"/>

                                </TextBlock>

                                <TextBlock FontSize="15">

                                    <Run Text="Дата заказа: "/>
                                    <Run Text="{Binding OrderDate, Mode=OneWay}"/>

                                </TextBlock>

                            </StackPanel>

                            <Border Grid.Column="1"
                                    Margin="15,0,0,0"
                                    BorderBrush="LightGray"
                                    BorderThickness="1"
                                    Padding="15"
                                    VerticalAlignment="Stretch">

                                <StackPanel VerticalAlignment="Center">

                                    <TextBlock Text="Дата выдачи"
                                               FontSize="15"
                                               FontWeight="Bold"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,5"/>

                                    <TextBlock Text="{Binding DeliveryDate, Mode=OneWay}"
                                               FontSize="18"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,15"/>

                                    <Button Content="Редактировать"
                                            Height="35"
                                            Margin="0,0,0,8"
                                            Background="#B8860B"
                                            Foreground="White"
                                            Visibility="{Binding DataContext.IsAdmin,
                                                                 RelativeSource={RelativeSource AncestorType=ListBox},
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Command="{Binding DataContext.EditOrderCommand,
                                                              RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"/>

                                    <Button Content="Удалить"
                                            Height="35"
                                            Background="#B8860B"
                                            Foreground="White"
                                            Visibility="{Binding DataContext.IsAdmin,
                                                                 RelativeSource={RelativeSource AncestorType=ListBox},
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Command="{Binding DataContext.DeleteOrderCommand,
                                                              RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"/>

                                </StackPanel>

                            </Border>

                        </Grid>

                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

    </Grid>
</Page>